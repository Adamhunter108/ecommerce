
// reducers are functions that takes in the current state and an action type what to do with the state and will update the store

const produtListReducers (state ={ products: [] }, action) => {
    case 'PRODUCT_LIST_REQUEST':
}